-- LazyVim loads this file before lazy.nvim starts.
-- Keep the first version intentionally small; add options only when needed.

local opt = vim.opt

-- Make the initial transition from a GUI editor comfortable.
opt.mouse = "a"
opt.clipboard = "unnamedplus"
opt.colorcolumn = "80,120"
