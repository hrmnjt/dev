-- Options are auto-configured by LazyVim
-- Override or add your own here

local opt = vim.opt

opt.relativenumber = true   -- relative line numbers
opt.scrolloff = 10          -- keep 10 lines above/below cursor
opt.colorcolumn = "80"      -- ruler at 80
opt.textwidth = 80          -- reflow at 80
opt.tabstop = 4             -- display tabs as 4 spaces
opt.shiftwidth = 2          -- indent by 2
opt.expandtab = true        -- tabs → spaces
opt.clipboard = "unnamedplus" -- system clipboard
